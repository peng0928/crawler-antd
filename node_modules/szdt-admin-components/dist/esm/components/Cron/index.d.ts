import React from 'react';
import './index.less';
interface Props {
    /**
     * @description 默认显示的cron
     * */
    value?: string;
    /**
     * @description 改变后回调
     * */
    onChange?: (v?: string) => void;
}
declare const Cron: React.FC<Props>;
export default Cron;
